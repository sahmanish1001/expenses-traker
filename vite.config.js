// Minimal Vite config. Defaults already match this project's layout:
//   - root: project root (where index.html lives)
//   - publicDir: "public" — served as-is at "/", untouched by the bundler.
//     That's where /main.js and /styles/main.css live for now, so this
//     first pass changes nothing about how the app runs — it's the same
//     plain <script>/<link> tags as before, just split into their own
//     files instead of inlined in index.html.
export default {};
